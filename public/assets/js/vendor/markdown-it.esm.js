import './markdown-it.min.js';

/** @type {typeof import('markdown-it')} */
const markdownit = /** @type {typeof import('markdown-it')} */ (/** @type {any} */ (globalThis).markdownit);

export default markdownit;
